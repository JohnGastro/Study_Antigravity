# Lesson 2.3: スクロールイベント

## 🔑 キーワード
- **windowオブジェクト** - ブラウザの画面全体
  - `document` はページの中身（HTML）
- **scrollイベント** - スクロールしたときに発生
- **window.scrollY** - 縦方向のスクロール量（px）
- **条件分岐 (if)** - 「もし〜なら」
- **classList.add / remove** - クラスをつけて/消してスタイルを変える

---

## 💡 あなたの気づき

### 「wってなんだ」
- **疑問**: `window` って何？
- **理解**: 画面全体のこと。スクロールバーなどは `document` ではなく `window` に属する。

### 「これは？」（const window = ...）
- **失敗**: `const window = ...` と書いてしまった
- **学び**: `window` は最初からある特別な名前（予約語）。自分で作らなくていい。

### 「まちがえちゃったっぽい」（box.classlist...）
- **失敗**: 変数名の間違い（`box` vs `target`）、スペルミス（`classlist` vs `classList`）、ロジック（`toggle` vs `add/remove`）
- **修正**: 自分で気づいて修正できた。特に `toggle` だと点滅してしまうことに気づき、`add/remove` に変更したのは素晴らしい。

---

## 📝 重要なコード

### スクロール監視の基本セット
```javascript
// 画面全体(window)を監視
window.addEventListener('scroll', function() {
    // 今のスクロール量を表示
    console.log(window.scrollY);
});
```

### スクロール量に応じたスタイル変更
```javascript
window.addEventListener('scroll', function() {
    // 300px以上スクロールしたら
    if (window.scrollY >= 300) {
        target.classList.add('true'); // クラスをつける
    } else {
        target.classList.remove('true'); // クラスを消す
    }
});
```

---

## 🎯 成果物
- [scroll_practice.html](file:///Users/kazune/antigravity/Antigravityと勉強する場所/Study_Antigravity/phase2_javascript/scroll_practice.html) - スクロールで黄色くなるページ

---

## 🤔 難しかったポイント
- `window` の概念
- `if` 文のロジック組み立て
- 変数名の管理

---

## 🔄 復習のヒント
- 「画面全体」のことは `window` に聞く
- 「条件によって切り替える」ときは `if` と `add/remove` の組み合わせが鉄板
